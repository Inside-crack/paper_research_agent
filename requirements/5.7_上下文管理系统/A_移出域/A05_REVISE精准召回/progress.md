# 需求实现进展

**需求ID**：A05  
**需求名称**：REVISE精准召回（断点续跑/步骤跳过）  
**优先级**：P2（延后至实验执行阶段）  
**当前状态**：⏸️ P2延后  
**创建日期**：2026-08-14

---

## 需求描述

A05最初定义为"REVISE精准召回"——REVISE时不加载上一轮完整对话，只加载精准的修正信息。

## 状态说明

**A05的核心功能（精准注入修正信息）已在A03中实现**：
- ✅ correction_notes包含eval issues（high/critical必保留）+ 修正要求
- ✅ 不保留上一轮完整对话历史（A03中context重置）
- ✅ 上一轮成功结果通过"已有数据"消息精准注入
- ✅ 旧LLM对话清空防锚定

**尚未实现的部分（断点续跑/步骤跳过）标记为P2**：
- 检测新Plan中的step与上一轮成功step参数一致时，跳过执行直接使用缓存结果
- 原因：当前工具（arXiv搜索等）是幂等的，重跑成本可接受（几秒）；断点续跑需要step参数匹配、缓存管理、plan diff逻辑，复杂度高
- 真正刚需断点续跑的场景是**实验执行阶段**（跑实验几分钟到几十分钟），届时再实现

## 核心验收点（已由A03覆盖的标记✅）

- [x] correction_notes中仅含：eval issues（severity>=high必保留）+ 修正要求 → ✅ A03的_build_correction_notes()
- [x] 不再重复注入上一轮的全部工具结果和对话历史 → ✅ A03中REVISE重置context
- [ ] 如果需要查看上一轮详细内容，通过load_artifact按需加载 → 基础设施A04已就绪（artifact_id已记录），但LLM在REVISE时不会主动调用工具（Synthesize阶段不支持工具调用），后续B03后再考虑
- [ ] 断点续跑：已成功step参数一致时跳过重复执行 → ⏸️ P2实验阶段

## 进度概览

- [x] 核心精准召回功能（A03中已实现）
- [ ] 断点续跑/步骤跳过 → P2
- [ ] LLM主动load_artifact召回详细数据 → 依赖B组（压缩替换）完成后再做

## 进展记录

### 2026-08-14
- 需求文档生成

### 2026-08-15
- A03 grill-me中确认：精准注入correction_notes+已有数据作为A03一部分实现
- 断点续跑标记为P2，延后至experiment_execution阶段（实验重跑成本高，才有断点续跑刚需）
- A04完成artifact自动落盘，为后续load_artifact召回提供基础设施
