# 设计决策

保存影响多个需求或多个模块的长期设计决策，例如：

- 持久化和恢复策略
- Agent 与 Orchestrator 的职责边界
- 论文 artifact 命名和版本策略
- 沙箱安全边界

单个需求的临时方案、卡点和解决过程仍记录在
`requirements/<需求目录>/solution.md`、`resolutions.md` 和 `blockers.md`，
不重复复制到这里。

命名格式：

```text
YYYY-MM-DD-<decision-name>.md
```
