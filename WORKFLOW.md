# 开发工作流规范（Harness Skills）

## 原则

**可以实现得慢，但是一定要稳。** 每一个需求都严格走以下四阶段流程，不跳步、不脑补、不攒bug。

---

## 阶段1：grill-me 需求澄清

**触发时机**：开始任何新功能/改动/修复之前，只要需求有任何模糊之处。

**核心目标**：把后面会翻车的问题提前问出来，写代码之前就搞清楚"做什么、不做什么、怎么算做完、出错了怎么办"。

**必问7维度**：
1. **需求定位与类型**：类型/模块/需求ID/优先级
2. **用户与场景**：谁用/哪个阶段触发/触发条件/入口
3. **功能规则与数据流**：输入/核心逻辑/输出/落盘位置/交互模块
4. **边界与异常**：Out of Scope/非法输入/工具失败/状态冲突/资源超限/硬约束冲突（重点追问负向路径）
5. **双Agent质量门禁适配**：是否需要Eval/确定性检查项/PASS/REVISE标准/修正范围
6. **验收标准**：可观测的验收点（文件/状态/测试/产物/Eval判定），必须包含负向用例
7. **影响面与风险**：回归范围/数据兼容/回滚方案

**退出条件**：输出《需求澄清纪要》，所有问题有明确答案，不存在"这个到底算不算"的疑问。

---

## 阶段2：openspec 规格沉淀

**触发时机**：grill-me 输出澄清纪要后，正式开始工程落地之前。

**核心目标**：把讨论结果固化成后续实现、review、验收必须对照的基线文档，避免"做着做着飘了"。

**输出四件套**（放在 requirements/ 对应需求目录下）：
- `solution.md`：规格+方案（核心基线），包含：
  - 背景/目标/In&Out of Scope
  - 核心功能规则（精确、可验证）
  - 阶段流转与状态变更
  - 边界条件与异常处理表（至少5个场景）
  - 双Agent质量门禁适配
  - 数据结构与产物
  - 验收标准（100%可验证，必须包含负向用例）
  - 技术方案/接口设计/影响面分析/风险评估
  - 里程碑任务拆分 T1/T2/T3（每个任务可独立验证）
- `progress.md`：进度跟踪初始化
- `blockers.md`：卡点记录初始化
- `resolutions.md`：决策记录初始化

**自检清单**：
- [ ] 所有章节填写完整，无"待补充"
- [ ] In/Out of Scope 边界清晰
- [ ] 至少5个异常场景有明确处理方式
- [ ] 双Agent门禁写清楚了
- [ ] 验收标准都是是/否可判断，包含至少2个负向点
- [ ] 任务拆成了里程碑级T1/T2/T3
- [ ] 影响面分析列出了所有要改的文件
- [ ] 其他三个文档已初始化

---

## 阶段3：superpowers 执行实现

**触发时机**：openspec 自检清单全部打勾后。

**核心目标**：稳扎稳打按规格实现，边做边验证，不攒bug、不私自改需求。

### 执行前准备
- 先把T1/T2/T3拆成细粒度PlanStep（每步30分钟内能做完+验证）
- 每个PlanStep必须包含：step_id/目标/对应T和spec条款/先读文件/要改的文件/验证方式/依赖
- 先跑一遍现有测试，确认基线是好的

### 单步执行循环（每步必须走完）
```
1. Read：把要改的文件全读一遍，理解现有逻辑和代码风格
2. Edit：小步局部修改，优先Edit不要Write整个文件，不顺手改无关代码
3. Verify：按验证方式马上验证，失败就debug这一步，不带着问题进下一步
4. Update：更新progress.md，遇到卡点记blockers.md，解决了记resolutions.md
```

### 硬约束（违反即不合格）
1. 增量修改优先，不为了"优雅"大重构
2. 修改数据结构时，新增字段必须 Optional + default，不破坏存量兼容
3. 严禁丢弃上游错误（`_ = err`/`except: pass`禁止）
4. 修复问题用局部低侵入方案，不改一堆无关代码
5. 确定性检查优先，数字/文件/退出码/JSON能用程序检查的不要靠LLM
6. **每个功能必须有负向测试**（非法输入怎么办）
7. 不提交无关文件（.env/__pycache__/日志等）
8. Research Agent synthesize 阶段不允许调用新工具（系统硬约束）
9. 遇到spec没写清楚的地方，停下来记录确认，不自己"合理"脑补

### 遇到问题处理
- **spec有缺口**：停下来写清楚缺口，给2-3个方案利弊，等确认再继续
- **技术卡点15分钟搞不定**：详细记录到blockers.md（做了什么尝试/完整错误/怀疑原因），可以先做其他步骤
- **发现现有无关bug**：不要顺手修，单独记录，除非它阻塞本次需求
- **计划有问题**：停下来调整计划，从正确步骤重新开始，不将错就错

---

## 阶段4：review-verify 验收对账

**触发时机**：superpowers 所有PlanStep完成，开发者说"做完了"。

**核心目标**：不凭感觉说"差不多做好了"，回到spec逐条对账，用证据说话。

### 验收流程（严格按顺序）

#### 第一阶段：前置检查（不通过直接BLOCKED）
- [ ] solution.md 是最新版本
- [ ] progress.md 显示100%完成
- [ ] `python -c "import paper_agent"` 导入无错误
- [ ] 所有现有测试通过（没有破坏原有功能）

#### 第二阶段：确定性检查（程序验证）
1. **文件与产物检查**：新增文件都存在、修改文件真的改了、产物格式合法、无多余文件
2. **测试运行检查**：所有验收测试跑了、退出码0、负向用例覆盖、边界场景测试
3. **数据兼容检查**：新字段兼容旧checkpoint、没有破坏性变更
4. **硬约束合规检查**：无丢弃错误、synthesize不调工具、修正次数逻辑正常、无无关改动

#### 第三阶段：Spec逐条对账（核心）
1. **功能规则对账**：solution.md 2.1节每条规则"当X时必须Y"，找代码位置、实际运行验证
2. **边界异常对账**：2.3节每个异常场景，实际制造异常验证处理方式
3. **质量门禁对账**：跑一遍E2E，看Evaluation实际输出
4. **验收标准逐条打勾**：每条要有证据（命令输出/文件内容/日志），负向点必须实际测

#### 第四阶段：代码质量与文档检查
- 代码风格一致、日志足够、职责清晰
- progress/blockers/resolutions 都更新了
- 实现中改了方案的话，solution.md 同步更新了

### 验收输出
- verdict: PASS / REVISE / BLOCKED
- score: 0.0-1.0
- issues清单（按阻塞/必须修正/建议优化分级）
- 验收点对账明细（每条带证据）
- REVISE时明确：只允许改哪些点、重验什么

**重要原则**：默认只允许一次REVISE，修正后局部重验，不无限循环返工。

---

## 工作流执行要求

1. **不跳步**：必须按顺序走grill-me → openspec → superpowers → review-verify，不能跳过澄清直接写代码
2. **每步有产出**：每个阶段结束必须有对应的文档输出，不能口头说"问过了"就开始
3. **文档和代码一致**：代码改了方案，必须同步更新solution.md，不能出现"代码和spec对不上"
4. **证据优先**：验收时所有结论必须有证据（文件路径/行号/命令输出/日志），不说"我觉得"、"看起来"
5. **慢就是稳**：宁可在前面澄清/规格阶段多花10分钟，也不要写到一半发现理解错了返工2小时

---

## 当前进度

这套工作流规范本身已经建立。从下一个需求（从上下文管理P0需求开始）正式严格执行：
1. 先走grill-me把第一个P0需求（A01阶段间上下文隔离）问透
2. 再走openspec更新solution.md成正式规格
3. 然后superpowers拆步执行
4. 最后review-verify验收
